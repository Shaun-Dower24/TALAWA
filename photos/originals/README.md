# Original photographs

Full-resolution files as supplied. Nothing here is used by the site directly —
`mockup/assets/` holds the resized, web-optimised copies that the page loads.

| File | Size | Used for |
|---|---|---|
| `tray.jpeg` | 1500×1804 | The tray sequence, and the hero background |
| `truck.jpeg` | 1536×2048 | Gallery panel 01 |
| `logo.jpeg` | 1254×1254 | Nav mark and hero eyebrow |
| `banner.jpeg` | 2048×1333 | Footer sign-off |

To regenerate the web copies, resize to the widths in `mockup/assets/` at
JPEG quality ~76–88 (Pillow, `Image.LANCZOS`, progressive, optimised).

## Still wanted

- A **landscape, top-down** tray shot in daylight. The current one is portrait
  and angled under indoor light; it works, but the tray sequence fills the
  screen and would be noticeably better with a flat overhead frame.
- The remaining shots listed on the gallery panels.
- A close-up of a menu board, or the prices typed out — the boards are ~80px
  wide in the truck photo and cannot be read.
